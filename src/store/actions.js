// import { getAppName } from '_api/app'

const actions = {
  // async getAppName ({ commit }) {
  //   try {
  //     const { info: { appName } } = await getAppName()
  //     commit('SET_APP_NAME', appName)
  //   } catch (err) {
  //     console.log(err)
  //   }
  // }
}

export default actions
